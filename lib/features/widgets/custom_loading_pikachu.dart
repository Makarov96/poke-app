import 'package:flutter/material.dart';

class CustomLoadingPikachu extends StatelessWidget {
  const CustomLoadingPikachu({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
